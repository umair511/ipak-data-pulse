// SPDX-License-Identifier: MIT
// Copyright (C) 2026 Shogo Technologies, Inc.
/**
 * Auto-generated Client Functions
 *
 * Fetch-based CRUD operations that call the Hono REST API.
 * No server-side imports — safe for browser bundles.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  MachineType,
  MachineCreateInput,
  MachineUpdateInput,
  FilmCodeType,
  FilmCodeCreateInput,
  FilmCodeUpdateInput,
  DowntimeReasonType,
  DowntimeReasonCreateInput,
  DowntimeReasonUpdateInput,
  ProductionEntryType,
  ProductionEntryCreateInput,
  ProductionEntryUpdateInput,
  TargetType,
  TargetCreateInput,
  TargetUpdateInput,
  ExportQuantityType,
  ExportQuantityCreateInput,
  ExportQuantityUpdateInput,
  PackingCostType,
  PackingCostCreateInput,
  PackingCostUpdateInput,
  CustomerType,
  CustomerCreateInput,
  CustomerUpdateInput,
  DispatchType,
  DispatchCreateInput,
  DispatchUpdateInput,
  AuditLogType,
  AuditLogCreateInput,
  AuditLogUpdateInput,
  UserPermissionType,
  UserPermissionCreateInput,
  UserPermissionUpdateInput,
  UserPlantType,
  UserPlantCreateInput,
  UserPlantUpdateInput,
} from './types'

/** Get the API base URL */
function getApiBase(): string {
  if (typeof window !== 'undefined') {
    return window.location.origin
  }
  return process.env.API_URL || 'http://localhost:3001'
}

// ============================================================================
// User Client Functions
// ============================================================================

/**
 * List all User records
 */
export async function getUserList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<UserType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/users${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list User')
  }
  const json = await response.json()
  return (json.items || []) as UserType[]
}

/**
 * Get a single User by ID
 */
export async function getUserById(args: { data: { id: string; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'User not found')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Create a new User
 */
export async function createUser(args: { data: { input: UserCreateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Update an existing User
 */
export async function updateUser(args: { data: { id: string; input: UserUpdateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Delete a User
 */
export async function deleteUser(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete User')
  }
  return { success: true }
}

// ============================================================================
// Machine Client Functions
// ============================================================================

/**
 * List all Machine records
 */
export async function getMachineList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<MachineType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/machines${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Machine')
  }
  const json = await response.json()
  return (json.items || []) as MachineType[]
}

/**
 * Get a single Machine by ID
 */
export async function getMachineById(args: { data: { id: string; userId?: string } }): Promise<MachineType> {
  const url = `${getApiBase()}/api/machines/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Machine not found')
  }
  const json = await response.json()
  return json.data as MachineType
}

/**
 * Create a new Machine
 */
export async function createMachine(args: { data: { input: MachineCreateInput; userId?: string } }): Promise<MachineType> {
  const url = `${getApiBase()}/api/machines`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Machine')
  }
  const json = await response.json()
  return json.data as MachineType
}

/**
 * Update an existing Machine
 */
export async function updateMachine(args: { data: { id: string; input: MachineUpdateInput; userId?: string } }): Promise<MachineType> {
  const url = `${getApiBase()}/api/machines/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Machine')
  }
  const json = await response.json()
  return json.data as MachineType
}

/**
 * Delete a Machine
 */
export async function deleteMachine(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/machines/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Machine')
  }
  return { success: true }
}

// ============================================================================
// FilmCode Client Functions
// ============================================================================

/**
 * List all FilmCode records
 */
export async function getFilmCodeList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<FilmCodeType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/film-codes${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list FilmCode')
  }
  const json = await response.json()
  return (json.items || []) as FilmCodeType[]
}

/**
 * Get a single FilmCode by ID
 */
export async function getFilmCodeById(args: { data: { id: string; userId?: string } }): Promise<FilmCodeType> {
  const url = `${getApiBase()}/api/film-codes/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'FilmCode not found')
  }
  const json = await response.json()
  return json.data as FilmCodeType
}

/**
 * Create a new FilmCode
 */
export async function createFilmCode(args: { data: { input: FilmCodeCreateInput; userId?: string } }): Promise<FilmCodeType> {
  const url = `${getApiBase()}/api/film-codes`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create FilmCode')
  }
  const json = await response.json()
  return json.data as FilmCodeType
}

/**
 * Update an existing FilmCode
 */
export async function updateFilmCode(args: { data: { id: string; input: FilmCodeUpdateInput; userId?: string } }): Promise<FilmCodeType> {
  const url = `${getApiBase()}/api/film-codes/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update FilmCode')
  }
  const json = await response.json()
  return json.data as FilmCodeType
}

/**
 * Delete a FilmCode
 */
export async function deleteFilmCode(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/film-codes/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete FilmCode')
  }
  return { success: true }
}

// ============================================================================
// DowntimeReason Client Functions
// ============================================================================

/**
 * List all DowntimeReason records
 */
export async function getDowntimeReasonList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<DowntimeReasonType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/downtime-reasons${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list DowntimeReason')
  }
  const json = await response.json()
  return (json.items || []) as DowntimeReasonType[]
}

/**
 * Get a single DowntimeReason by ID
 */
export async function getDowntimeReasonById(args: { data: { id: string; userId?: string } }): Promise<DowntimeReasonType> {
  const url = `${getApiBase()}/api/downtime-reasons/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'DowntimeReason not found')
  }
  const json = await response.json()
  return json.data as DowntimeReasonType
}

/**
 * Create a new DowntimeReason
 */
export async function createDowntimeReason(args: { data: { input: DowntimeReasonCreateInput; userId?: string } }): Promise<DowntimeReasonType> {
  const url = `${getApiBase()}/api/downtime-reasons`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create DowntimeReason')
  }
  const json = await response.json()
  return json.data as DowntimeReasonType
}

/**
 * Update an existing DowntimeReason
 */
export async function updateDowntimeReason(args: { data: { id: string; input: DowntimeReasonUpdateInput; userId?: string } }): Promise<DowntimeReasonType> {
  const url = `${getApiBase()}/api/downtime-reasons/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update DowntimeReason')
  }
  const json = await response.json()
  return json.data as DowntimeReasonType
}

/**
 * Delete a DowntimeReason
 */
export async function deleteDowntimeReason(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/downtime-reasons/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete DowntimeReason')
  }
  return { success: true }
}

// ============================================================================
// ProductionEntry Client Functions
// ============================================================================

/**
 * List all ProductionEntry records
 */
export async function getProductionEntryList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ProductionEntryType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/production-entries${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list ProductionEntry')
  }
  const json = await response.json()
  return (json.items || []) as ProductionEntryType[]
}

/**
 * Get a single ProductionEntry by ID
 */
export async function getProductionEntryById(args: { data: { id: string; userId?: string } }): Promise<ProductionEntryType> {
  const url = `${getApiBase()}/api/production-entries/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'ProductionEntry not found')
  }
  const json = await response.json()
  return json.data as ProductionEntryType
}

/**
 * Create a new ProductionEntry
 */
export async function createProductionEntry(args: { data: { input: ProductionEntryCreateInput; userId?: string } }): Promise<ProductionEntryType> {
  const url = `${getApiBase()}/api/production-entries`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create ProductionEntry')
  }
  const json = await response.json()
  return json.data as ProductionEntryType
}

/**
 * Update an existing ProductionEntry
 */
export async function updateProductionEntry(args: { data: { id: string; input: ProductionEntryUpdateInput; userId?: string } }): Promise<ProductionEntryType> {
  const url = `${getApiBase()}/api/production-entries/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update ProductionEntry')
  }
  const json = await response.json()
  return json.data as ProductionEntryType
}

/**
 * Delete a ProductionEntry
 */
export async function deleteProductionEntry(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/production-entries/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete ProductionEntry')
  }
  return { success: true }
}

// ============================================================================
// Target Client Functions
// ============================================================================

/**
 * List all Target records
 */
export async function getTargetList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<TargetType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/targets${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Target')
  }
  const json = await response.json()
  return (json.items || []) as TargetType[]
}

/**
 * Get a single Target by ID
 */
export async function getTargetById(args: { data: { id: string; userId?: string } }): Promise<TargetType> {
  const url = `${getApiBase()}/api/targets/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Target not found')
  }
  const json = await response.json()
  return json.data as TargetType
}

/**
 * Create a new Target
 */
export async function createTarget(args: { data: { input: TargetCreateInput; userId?: string } }): Promise<TargetType> {
  const url = `${getApiBase()}/api/targets`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Target')
  }
  const json = await response.json()
  return json.data as TargetType
}

/**
 * Update an existing Target
 */
export async function updateTarget(args: { data: { id: string; input: TargetUpdateInput; userId?: string } }): Promise<TargetType> {
  const url = `${getApiBase()}/api/targets/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Target')
  }
  const json = await response.json()
  return json.data as TargetType
}

/**
 * Delete a Target
 */
export async function deleteTarget(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/targets/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Target')
  }
  return { success: true }
}

// ============================================================================
// ExportQuantity Client Functions
// ============================================================================

/**
 * List all ExportQuantity records
 */
export async function getExportQuantityList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<ExportQuantityType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/export-quantities${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list ExportQuantity')
  }
  const json = await response.json()
  return (json.items || []) as ExportQuantityType[]
}

/**
 * Get a single ExportQuantity by ID
 */
export async function getExportQuantityById(args: { data: { id: string; userId?: string } }): Promise<ExportQuantityType> {
  const url = `${getApiBase()}/api/export-quantities/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'ExportQuantity not found')
  }
  const json = await response.json()
  return json.data as ExportQuantityType
}

/**
 * Create a new ExportQuantity
 */
export async function createExportQuantity(args: { data: { input: ExportQuantityCreateInput; userId?: string } }): Promise<ExportQuantityType> {
  const url = `${getApiBase()}/api/export-quantities`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create ExportQuantity')
  }
  const json = await response.json()
  return json.data as ExportQuantityType
}

/**
 * Update an existing ExportQuantity
 */
export async function updateExportQuantity(args: { data: { id: string; input: ExportQuantityUpdateInput; userId?: string } }): Promise<ExportQuantityType> {
  const url = `${getApiBase()}/api/export-quantities/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update ExportQuantity')
  }
  const json = await response.json()
  return json.data as ExportQuantityType
}

/**
 * Delete a ExportQuantity
 */
export async function deleteExportQuantity(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/export-quantities/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete ExportQuantity')
  }
  return { success: true }
}

// ============================================================================
// PackingCost Client Functions
// ============================================================================

/**
 * List all PackingCost records
 */
export async function getPackingCostList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<PackingCostType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/packing-costs${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list PackingCost')
  }
  const json = await response.json()
  return (json.items || []) as PackingCostType[]
}

/**
 * Get a single PackingCost by ID
 */
export async function getPackingCostById(args: { data: { id: string; userId?: string } }): Promise<PackingCostType> {
  const url = `${getApiBase()}/api/packing-costs/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'PackingCost not found')
  }
  const json = await response.json()
  return json.data as PackingCostType
}

/**
 * Create a new PackingCost
 */
export async function createPackingCost(args: { data: { input: PackingCostCreateInput; userId?: string } }): Promise<PackingCostType> {
  const url = `${getApiBase()}/api/packing-costs`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create PackingCost')
  }
  const json = await response.json()
  return json.data as PackingCostType
}

/**
 * Update an existing PackingCost
 */
export async function updatePackingCost(args: { data: { id: string; input: PackingCostUpdateInput; userId?: string } }): Promise<PackingCostType> {
  const url = `${getApiBase()}/api/packing-costs/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update PackingCost')
  }
  const json = await response.json()
  return json.data as PackingCostType
}

/**
 * Delete a PackingCost
 */
export async function deletePackingCost(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/packing-costs/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete PackingCost')
  }
  return { success: true }
}

// ============================================================================
// Customer Client Functions
// ============================================================================

/**
 * List all Customer records
 */
export async function getCustomerList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<CustomerType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/customers${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Customer')
  }
  const json = await response.json()
  return (json.items || []) as CustomerType[]
}

/**
 * Get a single Customer by ID
 */
export async function getCustomerById(args: { data: { id: string; userId?: string } }): Promise<CustomerType> {
  const url = `${getApiBase()}/api/customers/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Customer not found')
  }
  const json = await response.json()
  return json.data as CustomerType
}

/**
 * Create a new Customer
 */
export async function createCustomer(args: { data: { input: CustomerCreateInput; userId?: string } }): Promise<CustomerType> {
  const url = `${getApiBase()}/api/customers`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Customer')
  }
  const json = await response.json()
  return json.data as CustomerType
}

/**
 * Update an existing Customer
 */
export async function updateCustomer(args: { data: { id: string; input: CustomerUpdateInput; userId?: string } }): Promise<CustomerType> {
  const url = `${getApiBase()}/api/customers/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Customer')
  }
  const json = await response.json()
  return json.data as CustomerType
}

/**
 * Delete a Customer
 */
export async function deleteCustomer(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/customers/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Customer')
  }
  return { success: true }
}

// ============================================================================
// Dispatch Client Functions
// ============================================================================

/**
 * List all Dispatch records
 */
export async function getDispatchList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<DispatchType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/dispatches${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Dispatch')
  }
  const json = await response.json()
  return (json.items || []) as DispatchType[]
}

/**
 * Get a single Dispatch by ID
 */
export async function getDispatchById(args: { data: { id: string; userId?: string } }): Promise<DispatchType> {
  const url = `${getApiBase()}/api/dispatches/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Dispatch not found')
  }
  const json = await response.json()
  return json.data as DispatchType
}

/**
 * Create a new Dispatch
 */
export async function createDispatch(args: { data: { input: DispatchCreateInput; userId?: string } }): Promise<DispatchType> {
  const url = `${getApiBase()}/api/dispatches`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Dispatch')
  }
  const json = await response.json()
  return json.data as DispatchType
}

/**
 * Update an existing Dispatch
 */
export async function updateDispatch(args: { data: { id: string; input: DispatchUpdateInput; userId?: string } }): Promise<DispatchType> {
  const url = `${getApiBase()}/api/dispatches/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Dispatch')
  }
  const json = await response.json()
  return json.data as DispatchType
}

/**
 * Delete a Dispatch
 */
export async function deleteDispatch(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/dispatches/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Dispatch')
  }
  return { success: true }
}

// ============================================================================
// AuditLog Client Functions
// ============================================================================

/**
 * List all AuditLog records
 */
export async function getAuditLogList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<AuditLogType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/audit-logs${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list AuditLog')
  }
  const json = await response.json()
  return (json.items || []) as AuditLogType[]
}

/**
 * Get a single AuditLog by ID
 */
export async function getAuditLogById(args: { data: { id: string; userId?: string } }): Promise<AuditLogType> {
  const url = `${getApiBase()}/api/audit-logs/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'AuditLog not found')
  }
  const json = await response.json()
  return json.data as AuditLogType
}

/**
 * Create a new AuditLog
 */
export async function createAuditLog(args: { data: { input: AuditLogCreateInput; userId?: string } }): Promise<AuditLogType> {
  const url = `${getApiBase()}/api/audit-logs`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create AuditLog')
  }
  const json = await response.json()
  return json.data as AuditLogType
}

/**
 * Update an existing AuditLog
 */
export async function updateAuditLog(args: { data: { id: string; input: AuditLogUpdateInput; userId?: string } }): Promise<AuditLogType> {
  const url = `${getApiBase()}/api/audit-logs/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update AuditLog')
  }
  const json = await response.json()
  return json.data as AuditLogType
}

/**
 * Delete a AuditLog
 */
export async function deleteAuditLog(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/audit-logs/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete AuditLog')
  }
  return { success: true }
}

// ============================================================================
// UserPermission Client Functions
// ============================================================================

/**
 * List all UserPermission records
 */
export async function getUserPermissionList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<UserPermissionType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/user-permissions${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list UserPermission')
  }
  const json = await response.json()
  return (json.items || []) as UserPermissionType[]
}

/**
 * Get a single UserPermission by ID
 */
export async function getUserPermissionById(args: { data: { id: string; userId?: string } }): Promise<UserPermissionType> {
  const url = `${getApiBase()}/api/user-permissions/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'UserPermission not found')
  }
  const json = await response.json()
  return json.data as UserPermissionType
}

/**
 * Create a new UserPermission
 */
export async function createUserPermission(args: { data: { input: UserPermissionCreateInput; userId?: string } }): Promise<UserPermissionType> {
  const url = `${getApiBase()}/api/user-permissions`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create UserPermission')
  }
  const json = await response.json()
  return json.data as UserPermissionType
}

/**
 * Update an existing UserPermission
 */
export async function updateUserPermission(args: { data: { id: string; input: UserPermissionUpdateInput; userId?: string } }): Promise<UserPermissionType> {
  const url = `${getApiBase()}/api/user-permissions/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update UserPermission')
  }
  const json = await response.json()
  return json.data as UserPermissionType
}

/**
 * Delete a UserPermission
 */
export async function deleteUserPermission(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/user-permissions/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete UserPermission')
  }
  return { success: true }
}

// ============================================================================
// UserPlant Client Functions
// ============================================================================

/**
 * List all UserPlant records
 */
export async function getUserPlantList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<UserPlantType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/user-plants${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list UserPlant')
  }
  const json = await response.json()
  return (json.items || []) as UserPlantType[]
}

/**
 * Get a single UserPlant by ID
 */
export async function getUserPlantById(args: { data: { id: string; userId?: string } }): Promise<UserPlantType> {
  const url = `${getApiBase()}/api/user-plants/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'UserPlant not found')
  }
  const json = await response.json()
  return json.data as UserPlantType
}

/**
 * Create a new UserPlant
 */
export async function createUserPlant(args: { data: { input: UserPlantCreateInput; userId?: string } }): Promise<UserPlantType> {
  const url = `${getApiBase()}/api/user-plants`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create UserPlant')
  }
  const json = await response.json()
  return json.data as UserPlantType
}

/**
 * Update an existing UserPlant
 */
export async function updateUserPlant(args: { data: { id: string; input: UserPlantUpdateInput; userId?: string } }): Promise<UserPlantType> {
  const url = `${getApiBase()}/api/user-plants/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update UserPlant')
  }
  const json = await response.json()
  return json.data as UserPlantType
}

/**
 * Delete a UserPlant
 */
export async function deleteUserPlant(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/user-plants/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete UserPlant')
  }
  return { success: true }
}
